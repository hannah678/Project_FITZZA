<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.team.fitzza.dao.AlarmDAO">
	<select id="replyReceiverInfo" resultType="BoardVO">
		SELECT title, user_id FROM board WHERE board_num=${param1}
	</select>
</mapper>